import { Eye } from "lucide-react";
import { Button } from "@/components/ui/button";
import { StatusBadge } from "@/components/applicants/status-badge";
import type { Applicant } from "@/types/applicant";
import { formatDate, capitalize } from "@/utils/format";
export function ApplicantTable({ applicants, onSelect }: { applicants: Applicant[]; onSelect: (id: string) => void }) { return <div className="hidden overflow-hidden rounded-2xl border bg-card md:block"><table className="w-full text-left text-sm"><thead className="border-b bg-muted/40 text-muted-foreground"><tr>{["Applicant", "Country", "Position", "Status", "Applied", ""].map((heading, index) => <th key={heading || index} className="px-5 py-3 font-medium">{heading}</th>)}</tr></thead><tbody>{applicants.map((applicant) => <tr key={applicant.id} className="border-b last:border-0 hover:bg-muted/30"><td className="px-5 py-4"><p className="font-medium">{applicant.fullName}</p><p className="text-muted-foreground">{applicant.email}</p></td><td className="px-5 py-4">{applicant.country}</td><td className="px-5 py-4">{capitalize(applicant.track)}</td><td className="px-5 py-4"><StatusBadge status={applicant.status} /></td><td className="px-5 py-4 text-muted-foreground">{formatDate(applicant.applicationDate)}</td><td className="px-5 py-4"><Button variant="ghost" size="sm" onClick={() => onSelect(applicant.id)} aria-label={`View ${applicant.fullName}`}><Eye className="size-4" />View</Button></td></tr>)}</tbody></table></div>; }